extends layout 

block content 
  h1(class="text-center display-3 mb-4 jolly-font")= title 

  .container-fluid
    .row
      each genre in genreList
        .col-12.col-lg-6.col-xl-3
          .card.mb-4.d-flex.flex-column.align-items-center.justify-content-center.p-3
            .card-body.d-flex.flex-column.align-items-center
              h1(class="card-title") #{genre.name}
              a(href=genre.url, class="btn btn-primary") View

      else
        p There are no artists.